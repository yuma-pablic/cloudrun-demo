server:
 http_listen_port: 9080

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://alloy:12345/loki/api/v1/push

scrape_configs:
   - job_name: app
     static_configs:
      - targets:
          - localhost
        labels:
          job: app
          __path__: /var/log/app.log

     pipeline_stages:
      - json:
          expressions:
            level: level
            msg: msg
      - labels:
          level:
          msg: